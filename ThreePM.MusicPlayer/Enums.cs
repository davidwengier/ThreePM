namespace ThreePM.MusicPlayer
{
    public enum PlaylistStyle
    {
        Normal,
        Random,
        Looping,
        RandomLooping
    }

    public enum PlayerState
    {
        Stopped,
        Playing,
        Paused
    }
}
